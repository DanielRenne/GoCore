#!/bin/bash
whoami=$(whoami)
if [[ "$whoami" == "root" ]]; then
    ps -ax | grep "exe/goCoreAppTemplate" | awk '{print $1}' | xargs sudo kill  > /dev/null 2>&1
else
    ps -ax | grep "exe/goCoreAppTemplate" | awk '{print $1}' | xargs sudo kill  > /dev/null 2>&1
fi